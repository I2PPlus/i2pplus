plugins {
    id 'java-library'
}

sourceSets {
    main {
        java {
            srcDir 'src'
            srcDir "$buildDir/messages-src"
        }
    }
}

dependencies {
    api project(':core')
    api project(':router')
    api project(':installer')
    api project(':apps:systray')
}

// Create the java files from the po files. The jar task will compile them.
// This requires gettext 0.19 or higher.
// We don't support the "slow way"
// bundle-messages.sh exits fast when all locale classes are current
def buildDirPath = buildDir
task bundle(type: Exec) {
    workingDir rootDir
    commandLine 'sh', 'tools/scripts/bundle-messages.sh', '--dir', 'apps/desktopgui', '--build-dir', buildDirPath.absolutePath
}
compileJava.dependsOn bundle
jar.dependsOn bundle
