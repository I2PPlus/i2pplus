* On Windows, run the .exe file to start the installation process.

* On Linux and OSX, from a terminal run: java -jar i2pinstall.exe
  (from within the directory containing the file)

* To install headless (no GUI) on Linux and OSX, run:
  java -jar i2pinstall.exe -console